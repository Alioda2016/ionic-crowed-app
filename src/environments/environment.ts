// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase : {
    apiKey: "AIzaSyBpuv2TPsvbAvY4xaSOAfA28rkwG7b19rY",
    authDomain: "crowed-detection-app.firebaseapp.com",
    projectId: "crowed-detection-app",
    storageBucket: "crowed-detection-app.appspot.com",
    messagingSenderId: "905110734192",
    appId: "1:905110734192:web:da805e23e1c1af05e33bb5"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
